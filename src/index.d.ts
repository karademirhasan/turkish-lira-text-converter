/** Converts a non-negative Turkish lira amount into uppercase Turkish text. */
export declare function tryToTextConverter(amount: number | string): string;

/** Backward-compatible alias for tryToTextConverter. */
export declare const TryToTextConverter: typeof tryToTextConverter;
